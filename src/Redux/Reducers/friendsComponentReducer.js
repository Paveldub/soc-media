let initialState = {
  friends: [
    { id: 1, name: 'Pavel' },
    { id: 2, name: 'Ivan' },
    { id: 3, name: 'Artem' },
  ]
};

export const friendsReducer = (state = initialState, action) => {

  return state;
}